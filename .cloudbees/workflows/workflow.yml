apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: build-n-deploy

on:
  push:
    branches:
      - "main"
env:
  APP_NAME: simple-go-api-server
  AWS_REGION: us-east-1
  VERSION: 0.0.1

jobs:
  build:
    steps:
      - name: Say hello
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo "hello world 9"

      - uses: cloudbees-io/checkout@v1

      - name: build code
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          export GOPATH=$HOME/go
          export PATH=$PATH:$GOPATH/bin
          go version
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -ldflags="-w -s" -o build/api-server .

      - uses: cloudbees-io/configure-aws-credentials@v0
        id: aws-login
        with:
          aws-region: ${{ env.AWS_REGION }}
          aws-access-key-id: ${{ vars.aws_access_key_id }}
          aws-secret-access-key: ${{ secrets.aws_secret_access_key }}
          role-to-assume: service-ecr-role
          role-duration-seconds: "3600" # need to override default of 6h as our role has 1h max

      - uses: cloudbees-io/configure-ecr-credentials@v0

      - uses: cloudbees-io/kaniko@v1
        with:
          destination: ${{ steps.aws-login.outputs.aws-account-id }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.APP_NAME }}-image:${{ env.VERSION }}

      - id: helmpkg
        name: Package Helm chart
        uses: cloudbees-io/helm-package
        with:
          chart: ./charts/myapp
          destination: ./packaged-charts
          version: ${{ env.VERSION }}
          values: |
            image:
              repository:  ${{ steps.aws-login.outputs.aws-account-id }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.APP_NAME }}-image
              tag: ${{ env.VERSION }}

      - name: Publish Helm chart
        uses: cloudbees-io/helm-push
        with:
          chart: ${{ steps.helmpkg.outputs.chart }}
          remote: oci://${{ steps.aws-login.outputs.aws-account-id }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com

    outputs:
      chart-location: oci://${{ steps.aws-login.outputs.aws-account-id }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.APP_NAME }}
      chart-version: ${{ env.VERSION }}

  # scan:
  #   steps:
  #   - uses: cloudbees-io/checkout@v1
  #   - name: Security scan using snyk sca.
  #     uses: cloudbees-io/snyk-sca@v1.0.1
  #     with:
  #       orgname: "sdp-sandbox"
  #       token: ${{ secrets.SYNK_CLI_CLIENT_SECRET }}
  #       language: "LANGUAGE_GO"

  deploy:
    needs:
      - build

    steps:
      - uses: cloudbees-io/configure-aws-credentials@v0
        with:
          aws-region: ${{ env.AWS_REGION }}
          aws-access-key-id: ${{ vars.aws_access_key_id }}
          aws-secret-access-key: ${{ secrets.aws_secret_access_key }}
          role-to-assume: service-eks-role
          role-duration-seconds: "3600" # need to override default of 6h as our role has 1h max

      - uses: cloudbees-io/configure-eks-credentials@v0
        with:
          name: arch-saas

      - uses: cloudbees-io/configure-ecr-credentials@v0

      - id: install-chart
        name: Install helm chart
        uses: cloudbees-io/helm-install
        with:
          chart-location: ${{ needs.build.outputs.chart-location }}
          version: ${{ needs.build.outputs.chart-version }}
          release-name: app-chart
          namespace: ${{ env.APP_NAME }}
